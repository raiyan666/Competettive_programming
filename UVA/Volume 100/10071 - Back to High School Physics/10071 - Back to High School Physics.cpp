#include <bits/stdc++.h>

#define fast_in_out     ios_base::sync_with_stdio(false);cin.tie(0);
#define nl              printf("\n")
using namespace std;
int main()
{
    fast_in_out
    int a,b;
    while(cin>>a>>b)
    {
        int ans=2*a*b;
        cout<<ans<<'\n';;
    }
    return 0;
}
